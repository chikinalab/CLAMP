% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{CLAMPplotU}
\alias{CLAMPplotU}
\title{Plot the U matrix (pathway-LV associations) as a heatmap}
\usage{
CLAMPplotU(
  clampRes,
  auc.cutoff = 0.6,
  fdr.cutoff = 0.05,
  indexCol = NULL,
  indexRow = NULL,
  top = 3,
  sort.row = FALSE,
  cluster.rows = TRUE
)
}
\arguments{
\item{clampRes}{A CLAMP result list containing at least \code{U}, \code{Uauc}, \code{Up},
and \code{summary}.}

\item{auc.cutoff}{Minimum AUC threshold; entries below this are set to zero.
Default \code{0.6}.}

\item{fdr.cutoff}{Maximum FDR threshold for pathway significance filtering.
Default \code{0.05}.}

\item{indexCol}{Integer vector of LV column indices to include. \code{NULL} uses
all LVs.}

\item{indexRow}{Integer vector of pathway row indices to include. \code{NULL} uses
all pathways.}

\item{top}{Number of top pathways to retain per LV. Default \code{3}.}

\item{sort.row}{Logical; if \code{TRUE}, rows are sorted by the dominant LV.
Default \code{FALSE}.}

\item{cluster.rows}{Logical; if \code{TRUE} (default), rows are reordered by
hierarchical clustering (overridden when \code{sort.row = TRUE}).}
}
\value{
Invisibly returns a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Displays the pathway loading matrix \code{U} after filtering by AUC and FDR
thresholds.  Only the top-\code{top} pathways per LV are shown.
}
\examples{
set.seed(42)
pathways <- paste0("Path", 1:30)
lvs <- paste0("LV", 1:5)

U <- matrix(abs(rnorm(30 * 5)),
    nrow = 30,
    dimnames = list(pathways, lvs)
)
Uauc <- matrix(runif(30 * 5, 0.5, 1.0),
    nrow = 30,
    dimnames = list(pathways, lvs)
)
Up <- matrix(runif(30 * 5, 0, 3),
    nrow = 30,
    dimnames = list(pathways, lvs)
)

# Build a minimal summary table
nr <- 30 * 5
summ <- data.frame(
    pathway = rep(pathways, 5),
    LV = rep(lvs, each = 30),
    AUC = as.vector(Uauc),
    p_value = runif(nr, 0, 0.1),
    FDR = runif(nr, 0, 0.1),
    stringsAsFactors = FALSE
)

clampRes <- list(U = U, Uauc = Uauc, Up = Up, summary = summ)
CLAMPplotU(clampRes, auc.cutoff = 0.6, fdr.cutoff = 0.1, top = 3)
}
