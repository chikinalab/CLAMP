% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{select_svd_k}
\alias{select_svd_k}
\title{Select default number of components for a CLAMP solver SVD}
\usage{
select_svd_k(Y)
}
\arguments{
\item{Y}{A matrix-like object (dense matrix, \code{dgCMatrix}, or \code{FBM}).}
}
\value{
An integer: \code{max(2, floor((min(nrow(Y), ncol(Y)) - 1) / 4))}.
}
\description{
Returns the default number of components to compute in a truncated SVD
for the given input matrix. Used by the CLAMP solvers when \code{svd_k} is
not provided explicitly. Other SVD contexts use their own heuristics.
}
\examples{
select_svd_k(matrix(0, nrow = 100, ncol = 20))

}
