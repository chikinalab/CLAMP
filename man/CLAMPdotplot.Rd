% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{CLAMPdotplot}
\alias{CLAMPdotplot}
\title{Dot plot of top pathways for a single latent variable}
\usage{
CLAMPdotplot(
  clampRes,
  lv = 1,
  top = 20,
  auc.cutoff = 0.6,
  fdr.cutoff = 0.05,
  max.name.len = 50,
  x.axis = c("AUC", "-log10(FDR)", "log10FDR"),
  order.by = x.axis
)
}
\arguments{
\item{clampRes}{A CLAMP result list containing a \code{summary} data frame, or
the summary data frame itself.}

\item{lv}{LV to plot: either a numeric index (e.g. \code{1} -> \code{"LV1"}) or a
character name (e.g. \code{"LV3"}). Default \code{1}.}

\item{top}{Maximum number of pathways to display, chosen by \code{order.by}.
Default \code{20}.}

\item{auc.cutoff}{Minimum AUC to display. Default \code{0.6}.}

\item{fdr.cutoff}{Maximum FDR to display. Default \code{0.05}.}

\item{max.name.len}{Maximum characters for pathway label trimming.
Default \code{50}.}

\item{x.axis}{Metric to place on the x-axis. Use \code{"AUC"} or
\code{"-log10(FDR)"}. \code{"log10FDR"} is also accepted. Default \code{"AUC"}.}

\item{order.by}{Metric used to choose the top pathways and order the y-axis.
Use \code{"AUC"} or \code{"-log10(FDR)"}. \code{"log10FDR"} is also accepted. Defaults
to \code{x.axis}.}
}
\value{
Invisibly returns a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Lollipop-style dot plot showing the top pathways associated with one
selected LV. Dot size encodes AUC; dot colour encodes \code{-log10(FDR)}.
The x-axis and pathway ordering can use either AUC or \code{-log10(FDR)}.
}
\examples{
set.seed(9)
pathways <- paste0("Pathway_", 1:20)
lvs      <- paste0("LV", 1:5)
nr       <- length(pathways) * length(lvs)

summ <- data.frame(
  pathway = rep(pathways, length(lvs)),
  LV      = rep(lvs, each = length(pathways)),
  AUC     = runif(nr, 0.5, 1.0),
  FDR     = runif(nr, 0, 0.05),
  stringsAsFactors = FALSE
)

CLAMPdotplot(list(summary = summ), lv = 1, top = 10)
CLAMPdotplot(list(summary = summ), lv = "LV2", x.axis = "-log10(FDR)",
             order.by = "-log10(FDR)")
}
