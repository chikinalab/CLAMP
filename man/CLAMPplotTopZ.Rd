% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{CLAMPplotTopZ}
\alias{CLAMPplotTopZ}
\title{Plot top genes per LV by Z loading}
\usage{
CLAMPplotTopZ(
  clampRes,
  data = NULL,
  priorMat = NULL,
  top = 50,
  index = NULL,
  allLVs = FALSE,
  label.top = min(10, top),
  max.name.len = 50
)
}
\arguments{
\item{clampRes}{A CLAMP result list containing at least \code{Z}, and optionally
\code{U} for selecting LVs with pathway support.}

\item{data}{Deprecated; retained for backward compatibility and ignored.}

\item{priorMat}{Deprecated; retained for backward compatibility and ignored.}

\item{top}{Number of top genes to plot per LV. Default \code{50}.}

\item{index}{Integer or character vector of LV columns to include. \code{NULL}
keeps LVs with non-zero \code{U} entries when \code{U} is present.}

\item{allLVs}{Logical; if \code{TRUE}, all LVs are eligible when \code{index = NULL}.
Default \code{FALSE}.}

\item{label.top}{Number of top genes to label per LV. Default \code{min(10, top)}.}

\item{max.name.len}{Maximum characters for displayed gene labels.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object for one LV, or a \code{patchwork} object for
multiple LVs.
}
\description{
For each selected latent variable, ranks genes by their Z loading and plots
the top genes as a loading-versus-rank scatter plot. The highest ranking
genes are labelled with \code{ggrepel}.
}
\examples{
set.seed(1)
genes <- paste0("Gene", 1:80)
lvs   <- paste0("LV", 1:4)
paths <- paste0("Path", 1:10)
Z     <- matrix(abs(rnorm(80 * 4)), nrow = 80,
                dimnames = list(genes, lvs))
U     <- matrix(abs(rnorm(10 * 4)), nrow = 10,
                dimnames = list(paths, lvs))
clampRes <- list(Z = Z, U = U)
CLAMPplotTopZ(clampRes, top = 20, index = 1:2)
}
