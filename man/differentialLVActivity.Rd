% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{differentialLVActivity}
\alias{differentialLVActivity}
\title{Differential latent-variable activity between sample groups}
\usage{
differentialLVActivity(
  x,
  metadata,
  sample_col = "id",
  group_col = "type",
  reference
)
}
\arguments{
\item{x}{A CLAMP result list (with element \code{B}) or a numeric matrix
with latent variables in rows and samples in columns.}

\item{metadata}{Data frame with sample identifiers and group labels.}

\item{sample_col}{Name of the column in \code{metadata} holding sample
identifiers matching \code{colnames(B)}.}

\item{group_col}{Name of the column in \code{metadata} holding group labels.}

\item{reference}{Label of the reference group. All other samples are
treated as the comparison group.}
}
\value{
A data frame with one row per latent variable, ordered by FDR.
Columns: \code{LV}, \code{Mean_Reference}, \code{Mean_Comparison},
\code{Mean_Diff}, \code{P_Value}, \code{FDR}.
}
\description{
For each row of a CLAMP \code{B} matrix, compares mean activity in a
reference sample group against all other samples using a Wilcoxon
rank-sum test, with Benjamini–Hochberg FDR adjustment.
}
\examples{
B <- matrix(rnorm(30), nrow = 3)
rownames(B) <- paste0("LV", seq_len(3))
colnames(B) <- paste0("S", seq_len(10))
meta <- data.frame(
    id = colnames(B),
    type = rep(c("Control", "Case"), each = 5)
)
differentialLVActivity(B, meta, reference = "Control")

}
