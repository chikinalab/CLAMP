% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{CLAMPdotplotAll}
\alias{CLAMPdotplotAll}
\title{Dot plot of pathway-LV associations across all latent variables}
\usage{
CLAMPdotplotAll(
  clampRes,
  auc.cutoff = 0.6,
  fdr.cutoff = 0.05,
  top.per.lv = NULL,
  max.name.len = 40
)
}
\arguments{
\item{clampRes}{A CLAMP result list containing a \code{summary} data frame with
columns \code{pathway}, \code{LV}, \code{AUC}, and \code{FDR}.  Alternatively, \code{clampRes}
may be the summary data frame itself.}

\item{auc.cutoff}{Minimum AUC to display. Default \code{0.6}.}

\item{fdr.cutoff}{Maximum FDR to display. Default \code{0.05}.}

\item{top.per.lv}{Maximum number of pathways to display per LV, chosen by
highest AUC. \code{NULL} shows all. Default \code{NULL}.}

\item{max.name.len}{Maximum characters for pathway label trimming.
Default \code{40}.}
}
\value{
Invisibly returns a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Produces a dot plot where each point represents one pathway × LV pair.
Dot size encodes the AUC value and dot colour encodes \code{-log10(FDR)}. Only
associations passing \code{auc.cutoff} and \code{fdr.cutoff} are shown.
}
\examples{
set.seed(9)
pathways <- paste0("Pathway_", 1:20)
lvs      <- paste0("LV", 1:5)
nr       <- length(pathways) * length(lvs)

summ <- data.frame(
  pathway = rep(pathways, length(lvs)),
  LV      = rep(lvs, each = length(pathways)),
  AUC     = runif(nr, 0.5, 1.0),
  FDR     = runif(nr, 0, 0.2),
  stringsAsFactors = FALSE
)

CLAMPdotplotAll(list(summary = summ), auc.cutoff = 0.6, fdr.cutoff = 0.15)
}
