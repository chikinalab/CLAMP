% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{select_clamp_k}
\alias{select_clamp_k}
\title{Select default number of CLAMP latent variables from an SVD}
\usage{
select_clamp_k(
  svdres,
  n_samples,
  svd_k,
  method = c("elbow", "permutation", "gavish_donoho", "scaleSVs"),
  data = NULL,
  B = 20
)
}
\arguments{
\item{svdres}{An SVD result with a \code{d} component (output of \code{compute_svd}).}

\item{n_samples}{Integer number of samples in the original matrix
(i.e. \code{ncol(Y)}). Used by \code{"scaleSVs"} and \code{"gavish_donoho"}.}

\item{svd_k}{Integer upper bound on \code{clamp_k} (number of components
actually computed in the SVD).}

\item{method}{One of \code{"elbow"}, \code{"permutation"}, \code{"gavish_donoho"},
\code{"scaleSVs"}. Defaults to \code{"elbow"}.}

\item{data}{Raw data matrix. Required for \code{"permutation"} (row-normalized
internally) and \code{"gavish_donoho"} (used for \code{n_genes}).}

\item{B}{Number of permutations for \code{"permutation"}.}
}
\value{
An integer: the selected number of latent variables.
}
\description{
Chooses the default \code{clamp_k} used by the CLAMP solvers when the user
does not provide one, and returns the scale used for downstream L1/L2
regularization. Multiple methods are available via \code{method}:
}
\details{
\describe{
\item{\code{"elbow"} (default)}{Elbow heuristic on the singular-value spectrum
via \code{num.pc(svdres, method = "elbow")}. \code{scale = svdres$d[clamp_k]}.}
\item{\code{"permutation"}}{Permutation test via \code{num.pc(data, method = "permutation", B = B)}. Requires the raw row-normalized \code{data} matrix.
\code{scale = svdres$d[clamp_k]}.}
\item{\code{"gavish_donoho"}}{Gavish-Donoho optimal singular-value threshold
via \code{PCAtools::chooseGavishDonoho()}. Requires the raw \code{data} matrix
(used for \code{n_genes}). \code{scale = svdres$d[clamp_k]}.}
\item{\code{"scaleSVs"}}{Previous behavior: \code{getScaleFromSVs()} linear-tail fit,
\code{clamp_k <- min(floor(k * 1.5), svd_k)}, scale from the fit.}
}
}
