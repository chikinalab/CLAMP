% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{zscoreCLAMPFBM}
\alias{zscoreCLAMPFBM}
\title{Z-score a filtered FBM in-place}
\usage{
zscoreCLAMPFBM(fbm_filtered, rowStats, chunk_size = 1000, ncores = 1)
}
\arguments{
\item{fbm_filtered}{A bigstatsr::FBM produced by preprocessCLAMPFBM().}

\item{rowStats}{A list with row_means and row_variances from that FBM.}

\item{chunk_size}{Columns per block (default 1000).}

\item{ncores}{Integer; number of cores to use for parallel operations (default 1).}
}
\value{
A normalized FBM with z-scored rows.
}
\description{
Standardizes each row of an FBM using provided row means and variances.
}
\examples{
library(bigstatsr)
fbm <- FBM(
    nrow = 2, ncol = 4,
    init = matrix(seq_len(8), nrow = 2)
)
stats <- list(
    row_means     = rowMeans(fbm[]),
    row_variances = apply(fbm[], 1, var)
)
zscoreCLAMPFBM(fbm, stats, chunk_size = 2)
}
