# Row-wise correlation between two matrices

Computes the Pearson correlation for each row between matrices `A` and
`B`.

## Usage

``` r
row_cor(A, B)
```

## Arguments

- A:

  A numeric matrix.

- B:

  A numeric matrix of the same dimensions as `A`.

## Value

A numeric vector of correlations, one per row.
