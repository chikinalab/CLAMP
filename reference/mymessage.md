# Print a concatenated message

Wrapper around [`message()`](https://rdrr.io/r/base/message.html) that
pastes arguments together into a single string.

## Usage

``` r
mymessage(...)
```

## Arguments

- ...:

  Character strings to concatenate and print.

## Value

Invisibly returns NULL. Called for side effects (messages).
